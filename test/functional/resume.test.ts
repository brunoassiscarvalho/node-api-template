import supertest from 'supertest';

describe('Resume functional tests', () => {
  it('should return a resume whit just a few times', async () => {
    // const {body, status} = await supertest(app).get('/resume');
    expect(status).toBe(200);
    // expect(body).toBe()
  });
});
